<form class="{{classes}}">
    {{{title}}}
    {{#each inputs}}
        {{{this}}}
    {{/each}}
    <div class="{{styles.authorization-bottom}}">
        {{{submitButton}}}
        {{{loginLink}}}
    </div>
</form>