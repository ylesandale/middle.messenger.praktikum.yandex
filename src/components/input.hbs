<div class='input {{class}}'>
  <input
    type='{{type}}'
    name='{{name}}'
    placeholder='{{label}}'
    class='input__field'
    value='{{value}}'
  />
  <label class='input__label'>
    {{label}}
  </label>
  {{#if error}}
    <span class='input__error'>{{errorText}}</span>
  {{/if}}
</div>